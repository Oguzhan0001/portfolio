import type { Metadata } from "next";
import NoraPage from "./NoraPage";

export const metadata: Metadata = {
  title: "Nora Wellness — Project by Emre Aydın",
};

export default function Page() {
  return <NoraPage />;
}
